import Joi from 'joi';

export const create{{ModuleName}}Schema = Joi.object({
  name: Joi.string().required(),
});

export const get{{ModuleName}}Schema = Joi.object({
  id: Joi.string().required(),
});
